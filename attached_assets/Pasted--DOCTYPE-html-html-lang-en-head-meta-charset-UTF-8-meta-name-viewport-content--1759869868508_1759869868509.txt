<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>MasseurMatch</title>

  <!-- Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&family=Rowland:wght@700&display=swap" rel="stylesheet">

  <!-- Styles -->
  <style>
    * {margin:0; padding:0; box-sizing:border-box;}
    body {
      background-color:#0b0b0c;
      color:white;
      font-family:'Inter', sans-serif;
      min-height:100vh;
      display:flex;
      flex-direction:column;
      justify-content:center;
      align-items:center;
      text-align:center;
      overflow-x:hidden;
      padding:2rem;
    }

    /* Navigation */
    .sidebar-nav {
      position:fixed;
      top:2rem;
      left:2rem;
      display:flex;
      flex-direction:column;
      gap:1rem;
      z-index:100;
    }
    .sidebar-nav a {
      color:#fff;
      text-decoration:none;
      font-weight:600;
      font-size:0.95rem;
      transition:color 0.3s, transform 0.2s;
    }
    .sidebar-nav a:hover {
      color:#cfcfff;
      transform:translateX(4px);
    }

    /* Hero */
    .brand-title {
      font-family:'Rowland', sans-serif;
      font-size:3rem;
      font-weight:700;
      background:linear-gradient(90deg,#cfcfff,#ffffff);
      -webkit-background-clip:text;
      -webkit-text-fill-color:transparent;
      animation:fadeIn 1.5s ease forwards;
    }
    @keyframes fadeIn {
      from {opacity:0; transform:translateY(20px);}
      to {opacity:1; transform:translateY(0);}
    }

    .hero-typing {
      font-size:1.4rem;
      color:#bcbcff;
      font-weight:600;
      min-height:40px;
      margin-bottom:2rem;
      animation:fadeIn 2s ease 0.5s forwards;
    }

    /* Chat box */
    .chat-box {
      background:rgba(255,255,255,0.05);
      border:1px solid #333;
      border-radius:10px;
      padding:1rem;
      width:100%;
      max-width:480px;
      backdrop-filter:blur(10px);
      box-shadow:0 0 15px rgba(180,180,255,0.08);
      animation:fadeIn 2.2s ease 0.7s forwards;
    }
    .chat-box input {
      width:100%;
      background:#1a1a1f;
      border:1px solid #444;
      border-radius:6px;
      padding:0.75rem 1rem;
      color:white;
      font-size:1rem;
      outline:none;
      transition:border-color 0.3s;
    }
    .chat-box input:focus {
      border-color:#9a9aff;
    }

    /* Profiles */
    .featured-masseurs {
      display:flex;
      flex-wrap:wrap;
      justify-content:center;
      gap:1.5rem;
      margin-top:3rem;
      margin-bottom:4rem;
      animation:fadeIn 2.5s ease 0.9s forwards;
    }
    .profile-card {
      background:rgba(255,255,255,0.06);
      border:1px solid rgba(255,255,255,0.1);
      border-radius:12px;
      width:230px;
      text-align:left;
      padding:1rem;
      backdrop-filter:blur(8px);
      box-shadow:0 0 15px rgba(255,255,255,0.05);
      transition:transform 0.3s, box-shadow 0.3s;
    }
    .profile-card:hover {
      transform:translateY(-5px);
      box-shadow:0 0 20px rgba(188,188,255,0.15);
    }
    .profile-photo {
      width:100%;
      border-radius:8px;
      margin-bottom:0.5rem;
    }
    .profile-name {
      font-weight:700;
      font-size:1.1rem;
      color:#fff;
      margin-bottom:0.25rem;
    }
    .profile-info {
      font-size:0.9rem;
      color:#cfcfff;
    }

    /* Footer */
    footer {
      position:fixed;
      bottom:1rem;
      font-size:0.85rem;
      color:#888;
    }
  </style>
</head>
<body>

  <!-- Navigation -->
  <nav class="sidebar-nav">
    <a href="#">Home</a>
    <a href="#">Explore</a>
    <a href="#">Join Us</a>
    <a href="#">Who We Are</a>
    <a href="#">Login</a>
  </nav>

  <!-- Hero -->
  <main id="home">
    <h1 class="brand-title">MasseurMatch</h1>
    <div class="hero-typing">
      <span id="typing-text"></span>
    </div>

    <!-- Chat Box -->
    <div class="chat-box">
      <input type="text" placeholder="What kind of massage are you looking for?" />
    </div>

    <!-- Featured Profiles -->
    <section class="featured-masseurs">
      <div class="profile-card">
        <img src="https://via.placeholder.com/220x140" alt="Masseur 1" class="profile-photo" />
        <h3 class="profile-name">Alex • NYC</h3>
        <p class="profile-info">Starting at $120 · Deep Tissue, Swedish</p>
      </div>
      <div class="profile-card">
        <img src="https://via.placeholder.com/220x140" alt="Masseur 2" class="profile-photo" />
        <h3 class="profile-name">Marco • LA</h3>
        <p class="profile-info">Starting at $100 · Sports, Relaxation</p>
      </div>
      <div class="profile-card">
        <img src="https://via.placeholder.com/220x140" alt="Masseur 3" class="profile-photo" />
        <h3 class="profile-name">Tyler • Miami</h3>
        <p class="profile-info">Starting at $140 · Therapeutic, Outcall</p>
      </div>
    </section>
  </main>

  <!-- Footer -->
  <footer>© 2025 MasseurMatch — An Affirming Wellness Directory</footer>

  <!-- Typed Animation -->
  <script src="https://cdn.jsdelivr.net/npm/typed.js@2.0.12"></script>
  <script>
    const typed = new Typed("#typing-text", {
      strings: [
        "Find Affirming Wellness.",
        "Discover Trusted Hands.",
        "Connect With Inclusive Providers.",
        "Feel Seen. Feel Safe. Feel Better."
      ],
      typeSpeed: 45,
      backSpeed: 25,
      loop: true
    });
  </script>
</body>
</html>